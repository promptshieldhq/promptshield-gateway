version: 2

project_name: promptshield-gateway

before:
  hooks:
    - go mod tidy

builds:
  - id: promptshield-gateway
    main: ./cmd/gateway
    binary: promptshield-gateway
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}

archives:
  - id: promptshield-gateway
    builds:
      - promptshield-gateway
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: promptshieldhq
    name: promptshield-gateway
  draft: false
  prerelease: auto
